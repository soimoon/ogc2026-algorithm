__pycache__/
*.pyc
.venv/
*.egg-info/

# 대회 측이 배포한 인스턴스 JSON은 용량이 크고 organizer 자료라 굳이 커밋할 필요 없음
# (경로만 로컬에 두고 참조)
data/
*_instances*/
train-set*/

# built submission zips are derived artifacts (rebuildable from the tracked
# .py files) -- keep the folder locally as a submission log/history, but no
# need to commit the binaries themselves
submissions/