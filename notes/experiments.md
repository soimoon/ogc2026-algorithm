실험 기록 남기기.  
아래는 예시.  
## 2026-07-19
- baseline_greedy 그대로 복사, alg_tester로 스모크 테스트 통과 확인
- 관찰: bay_layout에서 블록이 시간축으로만 재사용, 공간 활용도 낮음

## 2026-07-2x
- _candidate_positions를 bounding_rect 대신 실제 폴리곤(check_collisions) 기반으로 교체 시도
- train prob_1~5 기준 objective 변화: ...

## 2026-07-19
- baseline_greedy로 prob_1(100 blocks, 2 bays) 실행, alg_tester Algorithm Output 로그 확인
  - Phase 1: 전 구간 `fallback=0` (100/100 블록 모두 탐색으로 자리를 찾음, 완전 배치 실패 없음)
  - Phase 2 repair: 대부분 `[time]`(시간만 조정), `[forced]` 1건(block65, 2회 연속 위반으로 사이클 방지용 강제배치) -- `best_score==inf`로 인한 진짜 fallback은 없었음
  - 결론: 이 인스턴스에서는 "공간이 없어서 못 들어가는" 수준의 실패는 발생하지 않음
- `analysis/shape_irregularity.py` 작성: 각 (block, orientation, layer)의 실제 다각형 면적 / bounding_rect 면적 비율 계산
  - `python analysis/shape_irregularity.py <instance.json>` 로 실행
  - prob_1 결과: 평균 비율 0.577, 중앙값 0.526, 최솟값 0.175 (block 25)
  - 분포: 43.5%가 비율 0.5 미만 (바운딩박스의 절반도 안 채움), 0.9 이상은 7.1%뿐
- 해석: `_candidate_positions`가 실패(fallback)로 이어지진 않지만, 블록 형상 자체는 평균적으로 바운딩박스의 40% 이상을 낭비하고 있음
  -> "실행 가능성" 문제는 아니지만 "패킹 품질/목적함수" 개선 여지는 충분해 보임 -> NFP(no-fit polygon) 스타일 후보 생성 시도해볼 가치 있음
- 다음: 위 `_candidate_positions` 교체 실험(바로 위 항목)을 이 근거를 바탕으로 진행

## 2026-07-21 -- 실채점 결과 기록 (그동안 기록이 안 남아있었음, 재발 방지용)

**1차 제출** (`submission-20260720-0516`, seed 미고정):
```
P1: 287,434
P2: 62,696
P3: 2,329,401
P4: 11,028,410
P5: 26,900,014
P6: 76,438,996
```

**2차 제출** (`submission-20260720-1937`, seed=0 고정):
```
P1: 13,310      (-95.4%, 21.6배 개선)
P2: 62,696      (0%, 완전 동일)
P3: 10,644,948  (+357%, 4.57배 악화 -- 원인 추적했으나 로컬에서 재현 못 함, notes/algorithm_overview.md 참고)
P4: 9,353,900   (-15.2%)
P5: 23,205,404  (-13.7%)
P6: 60,593,010  (-20.7%)
```

1차→2차는 P1의 압도적 개선(21.6배) 덕분에 P3의 큰 회귀(4.57배)를 감수하고도 순이익이었음. 이후 2026-07-21 로컬 8개 대표 인스턴스(180초) 기준 2차 제출 대비 오늘 변경사항 비교 결과(`analysis/before_after_compare.py`):
```
prob_1   -47.4% (회귀, 원인: 연산자 희석 -- 미해결, algorithm_overview.md #34)
prob_5     0%   (동일)
prob_9   +19.4%
prob_17   +2.65%
prob_20   +3.44%
prob_23   -0.07% (노이즈 수준)
prob_31  +18.6%
prob_40  +13.0%
```
(부호는 "+"=개선, "-"=악화로 표기 -- 위 실채점 표와 부호 방향이 반대이니 주의)
회귀 폭(1.47배)이 1차->2차 때의 P3(4.57배)보다 작고 승리 인스턴스도 더 많아, 3차 제출 판단 근거로 사용.
**주의**: P1~P6(숨겨진 채점 인스턴스)과 prob_1~40(로컬 train 인스턴스)은 서로 다른 인스턴스라 배율을 1:1로 정밀 비교할 수 없음 -- 패턴/자릿수 수준 참고만 가능.

**3차 제출** (`submission-20260721-0817`, wholebay ALNS 연산자 + diversity 필터 점수손실 기반 재설계):
```
P1: 15,740      (2차 대비 +18.3% 악화 -- 로컬 prob_1과 동일 패턴, 연산자 희석 가설 지지)
P2: 62,696      (0%, 3번 제출 전부 완전 동일)
P3: 2,654,378   (2차 대비 -75.1% 대폭 개선 -- 1차(2,329,401) 대비로도 +13.9%까지 근접, 사실상 해소)
P4: 9,343,224   (2차 대비 -0.1%, 거의 변화 없음)
P5: 22,200,804  (2차 대비 -4.3%)
P6: 52,892,073  (2차 대비 -12.7%)
```
**핵심 시사점**: P1 악화가 로컬에서 진단만 하고 못 고친 "연산자 희석" 문제(algorithm_overview.md #34)와 정확히 같은 방향으로 재현됨 -- 로컬 전용 현상이 아니라 실제 숨겨진 인스턴스에서도 나타나는 실재하는 문제라는 근거가 강해짐, 다음 라운드 우선순위로 격상. P3는 원인을 특정하지 못한 채로 개선됐음(diversity 필터 재설계 등 오늘 변경사항 전체의 부수효과로 추정, 정확한 원인 불명).
1차→3차 종합: P1(-94.5%)/P4(-15.3%)/P5(-17.5%)/P6(-30.8%) 전부 1차보다 개선, P2 동일, P3만 1차 대비 +13.9%로 소폭 악화(2차의 +357%에서 크게 회복).

**4차 제출** (`submission-20260721-2250`, 커밋 `e6b1a39`):

주요 변경사항(3차 대비, 상세는 algorithm_overview.md #35~42):
- ALNS 룰렛휠을 "패스당 1회 보장" 방식에서 진짜 가중치 복원추출로 재작성 (#35) -- 3차의 P1 회귀(연산자 희석) 근본 수정 목적
- 증분(incremental) feasibility 검증기 도입 + 하이브리드 컷오버 (#37, #38)
- candidate 생성 속도 대폭 개선: bay×orientation 완전 독립 스캔 예산 + AABB 순위 매기기 (#39), `_find_earliest_slot` 공간 사전 필터링 (#40, 손실 없음)
- stall 조건을 라운드 횟수에서 시간 비율(`STALL_TIME_FRAC=0.25`)로 전환 -- `_iterated_greedy` restart가 처음으로 제대로 발동하게 됨 (#41)
- **로컬 검증**: 8개 대표 인스턴스(180초, 3차 제출본 대비) 8전 8승, 다수 자릿수 단위 개선(prob_9 -98.5%, prob_20 -98.7%, prob_31 -98.9% 등). 40개 전체 로버스트니스 40/40 feasible·크래시 0·시간초과 0.
- **패키징 직전 사고 방지**: `check_feasibility_incremental`이 잘못 `utils.py`(채점 서버가 덮어쓰는 anti-cheat 파일)에 들어가 있던 것을 제출 직전 발견, `baseline_greedy.py`로 이전 + `utils.py` 원본 복원 (#42) -- 그대로 냈으면 전 인스턴스 ImportError로 전멸했을 뻔함.

기대: 3차의 P1 회귀(연산자 희석) 해소 + 전반적 품질 개선. 리스크: 오늘 변경 폭이 커서(특히 증분 검증기, candidate 생성 로직 변경) 로컬에서 못 잡은 엣지케이스 가능성 -- P2~P6 전체 결과로 판단 필요.

**실채점 결과 (2026-07-22 도착), 3차 대비**:
```
P1: 15,740 → 18,920    (+20.2% 악화)
P2: 62,696 → 54,696    (-12.8% 개선)
P3: 2,654,378 → 318,021 (-88.0% 대폭 개선)
P4: 9,343,224 → 10,380,972 (+11.1% 악화)
P5: 22,200,804 → 21,268,024 (-4.2% 개선)
P6: 52,892,073 → 53,599,248 (+1.3% 악화)
```
3승(P2/P3/P5) 3패(P1/P4/P6) -- "P3만 빼고 전부 악화"가 아니라 절반은 개선. 다만 가장 이상한 신호는 **P1이 1차 이후 계속 단조 악화**(13,310→15,740→18,920)한다는 점: #35/#36에서 "연산자 희석 문제를 근본 수정했다"고 로컬 `prob_1`(41,183 재현)으로 검증까지 했는데, 실제 숨겨진 P1은 오히려 더 나빠짐. 로컬 `prob_1`과 숨겨진 `P1`은 애초에 다른 인스턴스라(P1~P6과 prob_1~40은 1:1 대응 아님, 위 3차 항목 참고) "2차→3차 방향이 우연히 일치해서 같은 원인이라고 확정"한 이번 진단 자체가 성급했을 가능성이 높음.

**1순위 용의자**: 오늘 처음으로 실전에서 발동하기 시작한 "벽시계 시간(`time.time()`) 기반" 로직들 -- `STALL_TIME_FRAC=0.25`, `myalgorithm._iterated_greedy`의 재시작(최대 5회, 오늘 처음 실질적으로 여러 번 발동), `CANDIDATE_SCAN_CAP=50`. 이 셋 다 "라운드가 몇 초 걸리는가"에 실제 동작이 좌우되는데, 로컬 검증은 전부 사용자 개발 PC·180초 고정으로만 이뤄졌고, 실제 채점 서버(Ubuntu, AMD Threadripper PRO 9955WX, 4코어/16GB 강제 제한, 문제별 timelimit도 "몇 분~30분"으로 서로 다름 -- 문제 명세서 3.2절에 "개발 환경과 서버 스펙이 다르니 내부 elapsed time을 조심하라"고 명시적으로 경고됨)는 하드웨어/시간 조건이 전혀 다름. 재시작이 실제로 몇 번 도는지, STALL 임계값에 언제 걸리는지가 서버에서는 로컬과 다르게(더 적게, 혹은 운 좋게 더 많이) 나올 수 있음 -- P3의 극적 개선과 P1/P4/P6의 악화가 공존하는 것도 이런 "복불복" 패턴과 부합.

**P4/P5/P6이 4번 제출 내내 자릿수가 큰(수백만~수천만) 이유** (사용자 질문, 2026-07-22):
- 로컬 40개 인스턴스의 실제 가중치를 보면 `w1`이 667~29,630까지 다양하고 대부분 10,000~30,000대(`analysis`로 재확인 가능, 이 세션에서 직접 40개 인스턴스 JSON 로드해 확인함). `objective = w1*Z1 + w2*Z2 + w3*Z3`이므로, `w1`이 이 정도 크기면 총 지연시간(Z1)이 시간단위로 수백 정도만 돼도 objective는 수백만~천만 단위로 튀어 오름 -- **"목표값이 수백만"이라는 것 자체는 알고리즘이 나쁘다는 증거가 아니라, 그 인스턴스의 `w1`이 원래 크다는 것만으로도 충분히 설명됨.**
- 그런데도 주목할 점은 P4/P5/P6이 1~4차 내내 (버전이 완전히 다른 알고리즘이었음에도) **완만하게만 개선되는 패턴**이라는 것 -- P1처럼 요동치거나 P3처럼 롤러코스터를 타지 않고, 매번 조금씩만 줄어듦(P5: 26.9M→23.2M→22.2M→21.3M). 이건 Phase 3(ALNS)의 변화들(오늘까지 거의 모든 개선이 여기 집중됨)이 이 세 문제에는 별로 영향을 못 주고 있다는 신호로 보임 -- 가설: **P4/P5/P6은 Phase 1+2(초기 배치+repair)만으로 이미 주어진 timelimit의 대부분을 써버리는, 매우 크거나 혼잡한 인스턴스**라서 Phase 3가 사실상 거의 못 돌고, 그 결과 최근 세 차례 제출에서 ALNS 쪽 개선(연산자 재작성, 재시작 등)의 혜택을 거의 못 받는 것.
  - 방증: `_find_earliest_slot`의 위치 후보 생성이 "서로 다른 블록의 x/y를 교차곱해서 M×N으로 불어나는" 문제가 있다는 게 이미 알려져 있고(#40, 아직 미해결 TODO로 남음) 아직 해소가 안 됨 -- 블록 수/혼잡도가 커질수록 이 비용이 커서, 오늘 개선(스캔 캡+공간 필터)에도 불구하고 진짜 큰 인스턴스에서는 Phase 1조차 buget을 다 못 쓰고 넘어갈 위험이 남아있음.
  - 로컬에서 가장 큰 인스턴스(prob_40, 250블록)에서도 오늘 재시작이 실제로 발동해 -71.9% 개선이 났는데(#41), 이는 250블록 규모까지는 오늘 개선이 통했다는 뜻 -- 만약 숨겨진 P4/P5/P6이 이보다 한참 더 크거나 혼잡하다면(이 규모까지는 검증 안 됨), 같은 개선이 씨알도 안 먹혔을 수 있음.
  - **주의**: 사용자가 지적한 대로, 예제 인스턴스 스펙상 이론적으로 Z1=0(지연 없음)도 가능하지만, 숨겨진 P4/P5/P6의 실제 하한이 그렇게 낮으리라는 보장은 없음 -- 위 가설이 맞다면 "알고리즘이 아직 Phase 3에 도달을 못 해서" 개선 여지가 있다는 뜻이고, 반대로 물리적으로 정말 혼잡해서 Phase 3가 돌아도 못 줄이는 구조라면(진짜 하한이 높음) 이건 알고리즘 문제가 아니라 인스턴스 자체의 한계.
- **다음 검증 아이디어**: 로컬에서 가장 크고 혼잡한 인스턴스(prob_38/40 등)를 실제 서버급 timelimit(예: 최대치인 1800초)로 돌려 Phase 1/2가 전체 예산의 몇 %를 쓰는지, Phase 3가 조금이라도 도는지 확인 -- 만약 그 규모에서도 Phase 1/2가 이미 대부분을 먹는다면 위 가설을 강하게 뒷받침. 아직 실행 안 함.

**다음 제출 전 우선순위**:
1. 연산자 희석 가설은 폐기/보류 -- 이번 "수정 확인"의 근거가 무효화됐으므로, 다음 회귀 시 로컬 인스턴스 하나로 성급히 원인 확정하지 않기.
2. 시간 비율 기반 로직(`STALL_TIME_FRAC`, 재시작 예산 등)을 다양한 timelimit(60s/300s/1200s 등)과 CPU 제한 조건(`taskset`/`cpulimit`로 4코어 흉내)에서 재검증해 서버 조건에 대한 민감도 확인.
3. 위 P4/P5/P6 가설(“Phase1/2가 예산을 다 먹는다”) 검증 -- 가장 크고 혼잡한 로컬 인스턴스를 긴 timelimit으로 돌려 Phase별 시간 소모 비율 측정.

## 2026-07-22 (이어서) -- P4/P5/P6 가설 3번 항목 검증 + candidate 생성 근본 재작업

위 3번(합성 스트레스 테스트로 Phase 1 시간 소모 비율 확인) 실행 결과: prob_40을 2x/4x/8x로 블록 복제한 합성 인스턴스에서, Phase 1이 timelimit을 5배(60s->300s) 줘도 항상 예산의 ~60%만 소진하며 대부분 블록이 강제배치로 빠진다는 걸 확인 -- 가설 확정. 상세 진단과 이후의 candidate 생성 알고리즘 재작업(corner-only 시도 실패 -> MaxRects 시도 -> sliver-pruning으로 성능 문제 해결 -> xpress_reinsert 쪽 재도전 실패 -> 최종적으로 인스턴스 크기 기준 적응형 스위칭으로 정착)은 분량이 많아 `notes/algorithm_overview.md` #43~#51에 전부 기록함.

**요약**: `_place_blocks`(Phase 1 직접 배치)에 한해 블록 수 300개 이상인 인스턴스에서만 MaxRects(sliver-pruning 적용) 후보 생성을 쓰고, 그 미만이거나 `xpress_reinsert`(Phase 2/3 조인트 재배치) 경로는 전부 기존 O(m²) 교차곱 유지. 실측: 500블록 스트레스 인스턴스 objective -44%, 1000블록 -23.5% 개선, prob_1(100블록) 등 소형 인스턴스는 완전히 원래 성능으로 보존. 부수적으로 `_improve`의 Z1-하한 도달 시 tardy/swap 연산자를 영구 제거하던 버그(사용자 발견)도 같이 수정 -- 자세한 내용은 algorithm_overview.md #49 참고.

**다음 세션 할 일**:
1. 40개 로컬 인스턴스 전체(60초, 적응형 스위칭 적용된 최신 코드)로 회귀 없는지 재검증 -- 특히 prob_20(N=300, 임계값 경계)의 결과로 `MAXRECTS_MIN_BLOCKS` 임계값(현재 300, 보수적으로 잡은 추정치) 재조정.
2. `notes/experiments.md`의 3번 항목(#43~51에서 실행됨)에 이어, 남은 1/2번 항목(연산자 희석 가설 폐기, STALL_TIME_FRAC의 서버 하드웨어 민감도 재검증)은 아직 미착수.
3. 5차 제출 준비 -- 오늘 변경분(적응형 candidate 생성 + Z1 연산자 버그 수정) 반영해서 패키징.

## 2026-07-22 (이어서 2) -- 4차 제출 대비 40개 A/B 검증 중 prob_10 27배 회귀 발견 -> Phase 1 "deadline 절벽" 버그 진단/수정 -> 5차 제출 준비

**계기**: 위 항목 1(40개 재검증)을 실행하던 중, 4차 제출본(커밋 `692f3cb`) 대비 오늘 오전 변경분(#43~57) 전체를 60초 A/B 비교(`analysis/before_after_compare.py`)한 결과가 **23승 14패 3무**로 깨끗하지 않았음. `prob_10`이 196,955 -> 5,413,025(~27배)로 특히 심하게 악화됐고, 회귀 14개 전부 300블록 미만(MaxRects 무관)으로 확인.

**진단**: `prob_10`을 동일 코드·`seed=0`로 반복 실행하니 200,567/5,413,025/14,639,014로 자릿수 단위로 요동 -- 원인은 `_place_blocks`의 `phase1_deadline` 판정이 완전한 이진(binary) 스위치였다는 것. `time.time()`이 deadline을 넘는 순간부터 남은 블록 전부가 탐색 0회로 `_force_place`행. 몇 초의 순수 머신 타이밍 노이즈(같은 코드, 같은 seed인데 rank 140까지는 완전 동일한 배치였다가 그 이후 wall-clock이 8초 가까이 벌어짐)가 "블록의 20%를 탐색 0회로 날리느냐 마느냐"를 갈랐음. 기존 `hard_deadline`/`near_done_frac=0.05` 구제책은 "남은 5% 이하"일 때만 발동해서 이번 같은 20% 뭉텅이는 구제 못 함.

**수정**: `OVERTIME_SCAN_CAP=5`(최우선 bay 1개로 제한) 도입, `deadline`~`hard_deadline` 구간을 "저비용이지만 0은 아닌 탐색"으로 재정의(`near_done_frac` 기반 5% 게이트 완전 제거, 남은 블록 수 무관하게 전부 적용). 감사 결과 `_repair`(Phase 2)에도 같은 절벽이 있었음(자체 `timelimit*0.75` 강제배치 트리거가 새 `hard_deadline`보다 먼저 발동해서 무용지물이었던 것도 같이 발견/수정, 0.75->0.85로 정합). 상세 코드/근거는 `notes/algorithm_overview.md` #58 참고.

**검증**: prob_10 fix 후 3회 반복 -> 199,013으로 수렴(이전 200,567~14,639,014 요동, before=196,955와 동률 수준). prob_39(250블록, congested)로 `_repair` fix 확인 -> 312,642,282 -> 64,998,998 (~5배 개선). 회귀/개선/동률 섞은 11개 대표 인스턴스 재비교 -> 10승 1패(`prob_1`의 "패"는 8회 반복 재현 시도에서 재현 안 됨 -- restart 1이 8번 모두 68,633으로 결정론적이었고 최종값이 68,633보다 나빠진 적이 한 번도 없어 노이즈로 결론, 그 중 3회는 8,041로 수렴해 "prob_1의 진짜 최선"까지 재확인). **최종적으로 40개 전체를 20초라는 매우 짧은 예산으로 로버스트 테스트(`analysis/robustness_check.py`) -> 40/40 feasible, 크래시/infeasible/시간초과 0건.**

**남은 리스크(의도적으로 안 건드림)**: `_improve`(Phase 3)의 greedy 폴백(K≤12, xpress 실패시만)도 `hard_deadline` 없이 같은 절벽 가능성이 있으나, 이미 Phase 3 예산 끝자락이라 확장 시 오히려 전체 timelimit 초과(TLE) 위험이 있어 보류. `regret`/`batched` 우선순위 경로도 동일 결함 있으나 둘 다 폐기된 비활성 경로라 우선순위 낮음.

**항목 2/3(STALL_TIME_FRAC 하드웨어 민감도, P4/P5/P6 긴 timelimit 검증)은 이번에도 착수 못 함 -- 다음 세션으로 재이월.**

이 세션 변경분(overtime 절벽 수정 2건)을 반영해 5차 제출 패키징 진행.

## 2026-07-22 (이어서 3) -- 5차 제출 실채점 결과: 거의 전 문제 악화, 전략 전환

**실채점 결과 (4차 대비)**:
```
P1: 18,920 -> 21,390    (악화)
P2: 54,696 -> 54,696    (동일)
P3: 318,021 -> 442,230  (악화)
P4: 10,380,972 -> 10,085,115 (거의 동일, 여전히 1000만대에 고착)
P5: 21,268,024 -> 21,268,024 (완전 동일, 여전히 2100만대에 고착)
P6: 53,599,248 -> 52,266,489 (거의 동일, 여전히 5200만대에 고착)
```
P2만 그대로고 나머지는 전부 악화 또는 무변화 -- 특히 P4/P5/P6은 오늘 하루 종일 한 작업(적응형 MaxRects, deadline 절벽 수정, same-bay-first 등) 전부 반영했는데도 **완전히 꿈쩍도 안 함**. 로컬에서 그렇게 꼼꼼히 A/B 검증했던 것들이 실채점에는 거의 영향을 못 미쳤다는 뜻 -- "로컬 검증 통과"가 더 이상 신뢰의 근거가 되기 부족하다는 게 이번 결과의 핵심 시사점.

**사용자가 제기한 5가지 회귀/고착 원인 가설** (다음 세션에서 각각 격리 검증 필요, 상세는 `ogc2026_p4p6_investigation` 메모리 참고):
1. **same-bay-first fast path가 블록을 혼잡한 bay에 구조적으로 가둠** -- 자기 bay만 먼저 검색해서 quota(20개)가 채워지면 다른 bay 스캔 자체를 생략. 혼잡한 bay는 "타이밍은 나쁘지만 공간적으로는 채울 수 있는" 후보가 흔해서 quota가 쉽게 차버리고, 결과적으로 Xpress MIP에 "다른(한가한) bay로 이사"라는 선택지 자체가 전달되지 않음 -- P4~6처럼 지연이 수천만 단위에서 안 움직이는 패턴과 정확히 부합하는 메커니즘. **오늘 세션에 새로 추가된 코드**라 유력 용의자 1순위.
2. **Phase 1+2가 거대/혼잡 인스턴스에서 여전히 예산 대부분을 다 씀** -- MaxRects는 Phase 1 자체 배치(`_place_blocks`)만 빠르게 만들 뿐, `xpress_reinsert.py`(Phase 2 repair의 joint 재배치, Phase 3 전체)는 항상 O(m²) 교차곱 그대로라 전혀 빨라지지 않음. 원래(오늘 아침) 진단과 동일한 근본 원인이 그대로 남아있을 가능성.
3. **`JOINT_MAX_K=12`의 조인트 재배치 비용**이 1000블록급 규모에서 라운드당 수십 초로 폭증 -- 로컬 500/1000블록 합성 스트레스 테스트에서 이미 실측된 패턴과 일치.
4. 1/2/3과 같은 근본 원인: same-bay-first는 "전체 bay 스캔이 느려서" 만든 건데, 그 전체 스캔 자체가 O(m²) 때문에 느린 게 진짜 문제 -- 이 둘은 서로 반대 방향의 트레이드오프라 한쪽만 고쳐선 안 풀림.
5. **`wholebay` 연산자**(`WHOLE_BAY_MAX_PER_BLOCK=40`)도 P4~6 규모의 단일 bay에선 candidate 생성 자체가 라운드 데드라인을 못 넘기고 매번 실패할 가능성 -- 로컬 검증(`bay_mip_probe.py`)은 K~96-170 규모에서만 확인됨.

**전략 전환 (사용자 지시)**: 선두권과 점수 격차가 커서 "로컬에서 안 나빠짐"만 확인하며 조금씩 개선하는 접근으로는 부족하다고 판단 -- 앞으로는 **브랜치에서 과감하고 구조적으로 다른 시도를 다양하게 해보고, 검증된 것만 `main`에 반영**하는 방식으로 전환. A/B 검증 원칙 자체는 유지, 바뀐 건 시도의 과감함과 작업 위치(브랜치 우선).

**다음 세션 할 일**: 위 5가지 가설을 하나씩 격리 검증(가능하면 실험 브랜치에서) -- 특히 same-bay-first를 끄고 대형 합성 스트레스 인스턴스에서 bay별 tardiness 분포/Phase별 시간 소모를 직접 로깅해서 확인. `xpress_reinsert.py`의 O(m²) candidate 생성에 대한 진짜 알고리즘적 해법(MaxRects는 이미 이 경로엔 부적합하다고 결론남, #47(b)/#50)이 근본적인 다음 과제로 보임.

## 2026-07-22 (이어서 4) -- #58과 같은 종류의 병목 4곳 추가 발견/수정 + emergency fallback 위험 실증 및 차단

x4/x8/x16 합성 스트레스 인스턴스로 위 5가지 가설을 검증하는 과정에서, #58(Phase 1 deadline 절벽)과 **완전히 같은 패턴("몇 번 부르는지"가 인스턴스 크기에 비례해서 안 잘림)의 병목을 4곳 더 발견하고 전부 수정**했다 -- 상세 근거/실측 수치는 `algorithm_overview.md` #59 참고:

1. `_force_place`의 `_aabb_gap_entry` -- exit_time 내림차순 정렬 + 첫 매치 즉시 break로 O(N) 전수조사 제거 (x16 Phase1 575s->75s)
2. `_top_candidates_for_block`(xpress_reinsert 후보생성) -- `max_source_blocks=50` 캡 (x4에서 후보생성 2.8배 개선)
3. `_try_rebalance_move` -- 아예 캡이 없었던 걸 발견, 같은 패턴 적용
4. `_left_justify`/`_right_justify` -- deadline이 `t_start` 기준 고정비율이라 이전 phase가 일찍 끝나면 여유를 그대로 물려받던 버그, `time.time()` 기준 독립 예산으로 교체 (x4에서 116s->16s, Phase3 라운드 4->14개)
5. `_improve` 진입 시 중복 `check_feasibility` -- Left/Right-justify가 이미 계산해둔 결과를 재사용하는 `known_result` 파라미터 추가 (diff 방식인 `check_feasibility_incremental`이 아니라 단순 재사용 -- 상태가 그 사이 안 바뀌므로)

**emergency fallback이 실제로 일어나고 있을 수 있음을 발견**: 위 조사 중 x16을 3회 반복하니 2/3이 Repair 타임아웃 -> restart 시간 부족 -> `_emergency_fallback`으로 낙하. 이 폴백은 완전 결정론적이라 코드가 바뀌어도 값이 절대 안 변하는데, **실제 5차 제출 P5가 4차와 정확히 21,268,024로 동일**했던 것(P4/P6는 매번 다름)이 이 메커니즘과 정확히 부합 -- 실채점에서 이미 벌어지고 있었을 가능성.

**수정**: `_repair`에 "최종 feasibility 보장" 단계 추가 (사용자 설계: 루프 종료를 78%로 앞당겨 2% 예비 확보, 위반 블록을 뽑아낸 뒤 순차적으로 하나씩 force-place, 이미 95% 넘겼으면 스킵해서 새 TLE 방지). 인위적으로 100블록 전부 충돌시켜 강제 트리거 -> 정상 작동 확인(feasible 복구). x16 10회 반복 -> **10/10 feasible, emergency fallback 0회**(이전 3회 중 2회 대비 확실한 개선).

**검증**: 매 fix 단계마다 40개 로컬 인스턴스 20초 로버스트 테스트 실행, 전부 40/40 feasible/크래시 0/시간초과 0. prob_1이 60초에서 8,041(원래 180초 이상에서만 나오던 "진짜 최선")을 찾은 것도 확인 -- Phase 3에 실제로 시간이 더 생긴 효과로 보임.

**아직 안 한 것**: same-bay-first(가설 1)는 손 안 댐, 다음 세션(또는 이어지는 이번 세션)에 별도 브랜치에서 진행 예정. wholebay의 MIP 자체 비용(후보 생성과 별개)도 미해결. NFP(No-Fit Polygon) 도입도 사용자가 제안 -- 이번엔 "더 빽빽하게 채우기"(2026-07-20에 투자가치 없음으로 이미 결론난 각도)가 아니라 "충돌검사 자체를 더 싸게 만들기"(속도 각도)라는 다른 프레이밍으로, same-bay-first 다음 별도 브랜치에서 진행 예정.

이 세션 변경분(오늘 총 7개 fix)을 main에 커밋.

## 2026-07-22 (이어서 5) -- same-bay-first 결론, Improve 배치 확인, NFP 프로파일링 검증, check_feasibility 중복 제거 + bbox 캐싱

- **same-bay-first(가설 1) 최종 결론**: 블록 복제 스트레스(x4/x8)에서는 ON/OFF 우열이 배율에 따라 뒤집혀 신뢰 불가 -> 서로 다른 14개 로컬 인스턴스를 합친 3400블록 이질적 스트레스 인스턴스로 재검증하니 ON이 4회 전부 일관되게 4-15% 우세. P4~6 고착의 원인에서 제외, 기본값 ON 유지 (`algorithm_overview.md` #60).
- **`_improve`의 매 accept 전체 재검증을 `CONFIRM_CHECK_INTERVAL=10`회마다 1회+반환 직전 무조건 1회로 배치** (사용자 제안, 오늘 쌓인 섀도검증 624건/불일치 0건 근거). 결함 주입(몽키패치로 거짓 objective 유발)으로 MISMATCH 감지 및 체크포인트 롤백 정상 작동 확인. combined_3400 300초 before/after: 124,500,588,192 -> 124,116,249,147 개선(단, 13라운드 이후 갈라진 거라 타이밍 노이즈 영향 완전 배제는 못 함). 40개 로버스트 40/40 feasible (`algorithm_overview.md` #61).
- **NFP 도입 전 프로파일링 검증** (사용자 제안 "측정 먼저"): combined_stress_3400(60초) cProfile 결과 Shapely 자체 비용은 전체의 ~7-8%뿐, 실제 병목은 `check_feasibility`(9회, 33%)와 우리 코드의 MaxRects 후보생성(`_candidate_positions_maxrects2`, 28%, Shapely 미사용 순수 AABB 산술) -> **NFP 투자 대비 효과 낮다고 판단, 보류**.
- 사용자 재질문("check_feasibility가 진짜 그렇게 비싼 건지, 아니면 우리가 비효율적으로 여러 번 부르는 건지") 계기로 9회 호출 지점 전수 추적 -> Phase 2.5/2.6의 `pre_result`/`pre_rj_result` 2곳이 직전 단계가 이미 계산해둔 것과 완전히 동일한 순수 중복 호출이었음(Phase 2.6은 바로 옆의 `last_verified_result`를 아예 참조도 안 하고 있었음). `_repair()`가 `(assignments, verified_result)`를 반환하도록 바꾸고 Phase 2.5/2.6이 이를 재사용하도록 수정. 별도로 `_candidate_positions_maxrects2`가 이미 배치된 블록의 `bounding_rect()`를 매 호출 재계산하던 것을 Block 인스턴스에 캐싱(`_cached_bounding_rect`, x/y/orient_idx가 코드베이스 전체에서 in-place mutate 안 됨을 grep으로 확인 후 안전성 판단)하는 걸로 대체 (`algorithm_overview.md` #62).
- **검증**: combined_stress_3400 재프로파일링 -> check_feasibility 9->7회(정확히 2회 감소), bounding_rect 440만->324만 호출(~26%↓), 전체 60.0s->57.9s(~3.5%↓, 시간적응형 특성상 예상보다 작은 개선). 40개 로컬 인스턴스 20초 로버스트 -> 40/40 feasible, 0 크래시. `experiment/checkfeas-reuse-bbox-cache` 브랜치에서 main으로 merge.
- **다음 논의**: 시간 기반(wall-clock deadline) 아키텍처 자체의 안정성 문제 제기 -- 채점 서버는 1회만 채점하는데 타이밍 노이즈로 품질이 크게 흔들릴 수 있다는 우려. 3가지 후속 작업 합의: (1) 아직 이진 deadline cliff로 남은 곳 전수조사, (2) 로컬/서버 처리속도 차이를 자가보정하는 work-budget 설계, (3) safety margin(78%/95% 등)을 서버 스펙(4 CPU/16GB) 기준으로 재검토. 다음 세션(또는 이어지는 이번 세션)에서 순서대로 진행 예정.

## 2026-07-22 (이어서 6) -- 시간기반 아키텍처 안정성 재검토: cliff 전수조사 + scan budget 연속화

사용자 문제제기: "채점 서버는 1회만 채점하는데, 시간 기반(wall-clock) 아키텍처가 근본적으로 안정적일 수 있는가?" 3가지 후속 작업 합의(순서대로):

1. **전수조사**: `time.time() > deadline` 18곳 전부 확인 -> 새로운 이진 cliff 없음. 대부분 오늘 이미 고친 안전한 패턴이거나 죽은 코드(`_regret_construct`/`_place_blocks_batched`, `priority_rule`/`construction_mode`가 실제로 그 값으로 호출된 적 없음). `_improve`의 greedy 폴백만 미적용 상태로 남았으나 blast radius가 작아(K≤12, "개선 안 되면 버림" 로직) 기존 판단 유지.
2. **scan budget 연속화**: 사용자가 구체적 수식 제안(`time_left_ratio = (hard_deadline-now)/phase1_total_budget`, `dynamic_scan_cap = max(floor, int(base*ratio))`) -> `_dynamic_scan_cap()` 헬퍼로 구현, `_place_blocks`의 `OVERTIME_SCAN_CAP`/`CANDIDATE_SCAN_CAP` 이진 전환을 대체. x16 3회 반복 전부 feasible(63.2~63.8s), 40개 로버스트 40/40. `experiment/dynamic-scan-cap` -> main merge.
3. **측정 기반 안전마진**: 다음 작업으로 진행 예정 (`algorithm_overview.md` #64까지 기록됨, #65는 이 항목이 될 것).

**핵심 통찰**: 카운트 기반 캡(`CANDIDATE_SCAN_CAP` 등)은 사실 안전장치가 아니라 품질 레버였음 -- 어차피 매 K회 반복마다 `time.time()` 체크가 있어서 실제 소요 시간은 기계 속도와 무관하게 이미 timelimit 안에 묶여 있음. 진짜 위험한 건 이 캡이 "이진 전환"이라 경계에서 타이밍 노이즈로 요행이 갈리는 것.

## 2026-07-22 (이어서 7) -- 측정 기반 안전마진 (3번, 시간기반 아키텍처 안정성 재검토 마지막 단계)

`myalgorithm._solve`의 `inner_timelimit = timelimit*0.9`(check_feasibility 최종검증+emergency fallback용 고정 10% 예약)를, 인스턴스 크기(n_blocks) 기반 추정치와 기존 고정 10% 중 큰 쪽을 쓰도록 변경. 추정치 = 0.0013(오늘 프로파일링한 0.00065s/블록의 2배, 서버 속도 불확실성 헤지) * n_blocks * 3(남은 check_feasibility급 호출 수). `max()`라서 로컬 40개 인스턴스(최대 300블록)는 동작 변화 없고, 3400+블록에서는 추정치가 지배.

`_repair`의 78%/95%는 그대로 둠 -- 그건 check_feasibility가 아니라 force_place(이미 O(1)에 가까움, 진짜 변수는 "몇 개 강제배치되는지"라 사전 계측 불가) 비용을 위한 마진이라 같은 공식을 억지로 적용하는 게 부정직하다고 판단.

**검증**: x16/combined_stress_3400 각 3회 -> 전부 feasible, elapsed가 이전(60s 예산에서 63~64s로 초과하던 수준)보다 뚜렷이 개선되어 51~55s대로 예산 안에 안전하게 들어옴, objective는 동등 수준. 40개 로컬 로버스트 40/40. `experiment/measured-safety-margin` -> main merge.

이걸로 사용자가 요청한 3가지(cliff 전수조사/scan budget 연속화/측정기반 마진) 전부 완료. 다음: 사용자가 "check_feasibility 9번 호출도 사실 utils.py 탓이 아니라 우리 코드가 비효율적이었던 것"이라는 점을 짚으며, 지금까지의 로컬/스트레스 인스턴스 데이터로 전체 코드를 다시 훑어 "진짜 가장 심각한 병목"을 찾아보자고 제안 -- 오늘 적용한 수정들 때문에 프로파일이 이미 달라졌을 테니 combined_stress_3400/x16에서 재프로파일링부터 시작.

## 2026-07-22 (이어서 8) -- Block.bounding_rect() 전역 캐싱, 사용자 지적("check_feasibility 9번은 코드 비효율이었지 utils.py 탓 아니었잖아")에 대한 후속 작업

사용자가 오늘의 check_feasibility 중복 제거 건을 짚으며 "다른 큰 비용들도 사실 우리 코드 비효율일 수 있으니 재프로파일링해서 진짜 가장 심각한 병목을 찾자"고 제안. combined_stress_3400을 최신 코드(오늘 3개 fix 전부 반영)로 재프로파일링 후 `pstats.print_callers`로 `bounding_rect` 호출자 breakdown 확인.

결과: 306만 회 남은 bounding_rect 호출 중 170만 회(56%)가 `_find_earliest_slot`에서(오늘 오전 `_candidate_positions_maxrects2`에만 적용했던 캐싱이 여기 빠져있었음), 그리고 결정적으로 **check_feasibility(utils.py) 자체도 bay별 Block 리스트를 Stage 2/3/4 전체에서 재사용하면서 각 스테이지가 독립적으로 bounding_rect()를 또 호출** -- 개별 호출부 캐싱으론 닿을 수 없는 utils.py 내부 비용.

**수정**: 개별 호출부 캐싱(`_cached_bounding_rect` 래퍼)을 폐기하고, `baseline_greedy.py`가 import될 때 `Block.bounding_rect` 메서드 자체를 클래스 레벨에서 패치. utils.py 파일은 안 건드리고 런타임에 이미 로드된 클래스 객체에 캐싱 속성만 추가하는 방식이라, check_feasibility를 포함한 모든 코드가 자동으로 혜택을 받음.

**검증**: bounding_rect 호출 304만->30.6만(~90%↓), 상위 30위 밖으로 밀려남. x16/combined_3400 각 3회 -> 전부 feasible, combined_3400 objective가 143.7억~149.9억 -> 124억~128억으로 개선(절약된 시간이 Phase 3에 재투자됨). 40개 로버스트 40/40. `experiment/global-bbox-cache` -> main merge.

**남은 최대 병목**: `_candidate_positions_maxrects2`(MaxRects 후보생성) 자체의 O(m×freelist²) 재생 비용 -- 이건 캐싱 버그가 아니라 매 호출이 진짜 새 계산이라 생기는 알고리즘적 복잡도. 진짜 해법(free-rect 리스트를 호출 간 persistent 유지)은 코드에 이미 힌트가 남아있지만, sliver-pruning 임계값을 전역 최솟값 기준으로 바꿔야 하는 정확성 이슈 때문에 미뤄진 상태 -- 오늘 것들보다 크고 리스크 있는 별도 프로젝트로 남김. 사용자에게 이 프로젝트를 지금 할지 오늘은 여기서 멈출지 문의.

## 2026-07-22 (이어서 9) -- MaxRects orientation간 공유 (범위 축소된 버전), 그리고 Phase 1 시간 배분 재검토로 방향 전환

원래 계획(free-rect 리스트를 블록 간/호출 간 persistent)을 설계하다가 시간 의존 정확성 문제 발견: `active_in_bay`가 `e_k > r_time`으로 필터링되고 블록 처리 순서가 EDD(마감일)라 release_time 순서가 아니라서, "활성" 집합이 호출마다 비단조적으로 바뀜 -- 단순 persistent 캐시는 틀릴 수 있음(구간트리 같은 시공간 자료구조가 필요한 훨씬 큰 프로젝트). 사용자 확인 후 범위를 안전한 부분집합으로 축소: **같은 블록의 orientation간 공유**(시간 컨텍스트 완전히 동일, 문제 없음).

`_candidate_positions_maxrects2`를 `_maxrects_free_space`+`_maxrects_extract_candidates`로 분리, `_place_blocks`가 bay당 1회만 free-space를 계산(전역 min_width/min_height 사용, 항상 보수적이라 안전) 후 orientation 루프에서는 값싼 extract만 반복. 동등성 검증 스크립트로 기존 방식과 결과 후보 집합 비교 -> 0 불일치 확인 후 진행.

**실측은 애매함**: `_maxrects_free_space` 호출 8배 감소(32,618->4,164)했지만, 전역 최솟값이 더 보수적이라 호출당 유지하는 조각 수가 늘어 O(freelist²) pruning 비용이 커짐 -- 전체 wall-clock은 이전과 비슷한 수준(50s대), 순이익이 이번 샘플에서는 뚜렷하지 않음. 정확성은 확실(x16/combined_3400 각 3회 전부 feasible, 40개 로버스트 40/40) -> main merge, 하지만 순이익 재확인은 다음 과제.

**사용자가 더 근본적인 질문 제기**: "Phase 1은 원래 유효한 뼈대를 순식간에 만들어야 하는 거 아닌가?" -- 오늘 모은 로그에서 Phase1 elapsed/전체 elapsed 비율을 직접 뽑아보니 prob_1(100블록)=58%, combined_3400(3400블록)=53-55%, x16(4000블록)=50%로 **인스턴스 크기와 무관하게 절반 안팎으로 놀랍도록 일정** -- `phase1_deadline = t_start + timelimit*0.5`로 아예 못박혀 있어서 설계상 그런 것. 결정적으로 prob_1은 100블록 전부 `fallback=0`(강제배치 없이 정상 탐색)인데도 8.72초가 걸렸고 Phase 3는 예산이 남았는데도 5라운드 연속 무개선으로 스스로 멈춤 -- 즉 이 케이스에서 Phase1이 오래 걸린 건 데드라인에 밀려서가 아니라 블록당 순수 탐색 비용(~87ms/블록)이 실제로 그만큼 든 것. 오늘 한 캐싱류 최적화는 Phase1을 "빠르게"만 할 뿐 이 50% 천장 자체는 안 건드려서, Phase1이 이미 일찍 끝나는 게 아니면 체감 효과가 제한적 -- 다음 방향은 "Phase1의 50% 배분이 실제로 정당한가" 쪽으로 갈 가능성.
